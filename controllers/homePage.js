// const Post = require("..database/models/Article")
// const sliderHome = require('../controllers/script/scriptSlider')

module.exports = (req, res) => {
    // const posts = await Post.find({})

    res.render("index")
    // { posts }

}